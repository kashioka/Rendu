# cargo audit の設定 (CI の security-audit / security-gate / 週次スキャンは
# いずれも working-directory: src-tauri で実行されるため、このファイルが適用される)

[advisories]
# 現在 ignore 中の advisory はなし。
# ignore を追加するときは必ず「なぜ実リスクがないか」と「解除条件」をコメントで残し、
# トラッキング用の issue を立てること (過去の例: #99 quick-xml / #138 rkyv)。
ignore = []
